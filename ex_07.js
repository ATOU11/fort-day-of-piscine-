if (password == "forty-two") {
    // Si la variable password est égale à "forty-two", afficher "Success" avec une majuscule initiale, conformément aux consignes.
    displayThisText("Success");
} else {
    // Sinon, si le mot de passe est incorrect, afficher "Wrong password" avec la casse correcte.
    displayThisText("Wrong password");
}
